# Admin portal (required for /admin)
ADMIN_PASSWORD=your-secure-password
ADMIN_SECRET=random-string-for-jwt-signing-min-32-chars

# GitHub API (required to save changes from admin)
GITHUB_TOKEN=ghp_xxxx
GITHUB_REPO=owner/repo

# If the Next app lives in a subfolder of the repo (e.g. actingOutOK_react), set:
# GITHUB_PATH_PREFIX=actingOutOK_react
# Optional: branch to commit to (default: repo default branch)
# GITHUB_BRANCH=main

# TMDB (for Cast profile pages: photo + credits from IMDb)
# Get a free API key at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key

# Supabase (optional – when set, directory/resources/casting-calls use DB instead of JSON)
# Project URL from Dashboard → Project Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
# Service role key (secret) from same page – never commit or expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
